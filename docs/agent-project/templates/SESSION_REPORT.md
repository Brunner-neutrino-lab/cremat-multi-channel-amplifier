# Session Report — <track id>

> **The summary other tracks read instead of my log.** Keep current (overwrite). A
> consumer must be able to integrate from this + `INTERFACE.md` alone.

Track: `<id>` · Aspect: `<design|sim|models-bom>` · Status: `<not-started|in-progress|blocked(<on>)|criteria-met|COMPLETE>`
Last updated: `YYYY-MM-DD`

## Objective
One or two sentences: what this track delivers.

## Success / failure criteria
- ✅/⏳/❌ criterion 1 (measurable) …
- ✅/⏳/❌ criterion 2 …
(State these *before* the work; mark status as you go.)

## Current state
Where things stand vs. the criteria; current milestone (e.g. shaper M1 vs M2).

## Deliverables (what & where)
- `path/to/thing` — what it is.
- Plots: `path/to/plot.png` — figure of merit shown.

## Interface I expose / consume
- Expose: `<ports, ranges, schematic handle>` (or "see ../INTERFACE.md").
- Consume: `<which other reports/interfaces I depend on>`.

## How to use my output
One line for the downstream track.

## Open issues / asks
- … (e.g. "need a model downloaded behind login: <part> <link>").
