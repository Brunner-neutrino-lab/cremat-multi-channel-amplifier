# Session Report — A1 `csp-design`

> The summary other tracks read instead of my log.

Track: `A1 csp-design` · Aspect: `design` · Status: `COMPLETE (real parts; A3 gate passed, ERC 0/0 · DRC 0/0/0)`
Last updated: `2026-06-25`

## Objective
Design the standalone CR-112 CSP eval board including the SiPM bias front-end: schematic,
4-layer layout (place + FreeRouting), ERC + DRC to 0/0, publish `INTERFACE.md`. Generic
parts now; swap to real MPNs at the A3 gate.

## Success / failure criteria
- ✅ ERC 0 on the schematic (`reports/erc.json`: 0 errors / 0 warnings).
- ✅ DRC 0 on the 4-layer routed board (`reports/drc.json`: 0 errors / 0 warnings / 0 unconnected).
- ✅ Per-rail decoupling present per CR-150-R5: 4.7 Ω series + 10 µF + 0.1 µF on each of
  +Vs/−Vs at the CR-112 (R6/C4/C5 on +VS_F; R7/C6/C7 on −VS_F) + bulk C8/C9.
- ✅ Bias front-end with working 0R-bypass DNP: Rf1/Rf2 (R1/R3) fitted, JP_Rf1/JP_Rf2
  (R2/R4) 0R bypass flagged DNP (verified in `reports/bom.csv`).
- ✅ `INTERFACE.md` current (ports, ranges, schematic handle, DNP, **real-parts BOM table**).
- ✅ Real-parts board (Round 2): A3 MPNs swapped in 1:1, re-placed/re-routed, gates re-run.

## Current state
**REAL-PARTS board COMPLETE and clean: ERC 0/0, DRC 0/0/0, fully autorouted 4-layer**
(F.Cu / GND plane / −VDC plane / B.Cu). A3's chosen MPNs are set on every part (Value + MPN +
MFN + Footprint). Design BOM == A3 BOM for all 12 A3 lines; the only diff is the 47 Ω test
resistor (R5) that A3 omitted — A1 supplied a real Yageo RC0805 part and flagged the one row
for A3 to add. CR-112 lead spec verified against Cremat's own CR-150-R5 (`8pinSIP`, 2.54 mm
pitch) → generic SIP-8 footprint is correct, no change.

## Round-2 (real-parts gate) changes
- `gen_sch.py`: each `CH` row now carries real Value + MPN + Mfr (emitted as `MPN`/`MFN`
  symbol fields). Footprints unchanged except: rail bulk 10 µF C4/C6 (1206→0805, Samsung
  CL21A106KAYNNNE) and rail-entry bulk C8/C9 (generic 10 µF/1206 → **100 µF/25 V radial
  electrolytic** `Device:C_Polarized` + `Capacitor_THT:CP_Radial_D6.3mm_P2.50mm`, Nichicon
  UVR1E101MED).
- `gen_pcb.py`: re-spaced C8/C9 for the larger radial THT body (D6.3 mm, ~6.8 mm courtyard).
- Pipeline re-run: gen_sch → ERC 0/0 → netlist → gen_pcb (22/22 placed, pre-route DRC
  0/0/42) → export DSN → FreeRouting 2.2.4 (36 nets, 1.07 s, score 996.77, 0 unrouted) →
  import SES → fill_zones (4 zones) → **DRC 0/0/0**.

## Deliverables (what & where)
- `design/csp-cr112.kicad_sch` — schematic (generated by `design/gen_sch.py`).
- `design/csp-cr112.kicad_pcb` — routed 4-layer PCB (built by `design/gen_pcb.py`, routed by
  FreeRouting, zones filled by `design/fill_zones.py`).
- `design/csp-cr112.kicad_pro` — project (net classes + DRC severities).
- `design/csp-cr112.net` — exported netlist (verified node-by-node).
- `design/reports/erc.json`, `design/reports/drc.json` — gate evidence (0/0, 0/0/0).
- `design/reports/csp-cr112.svg` — schematic render; `design/reports/pcb-top.png` — 3D top.
- `design/reports/bom.csv` — **real-parts BOM** (Value + MPN + Manufacturer + Footprint;
  DNP column set on the 0R bypass jumpers R2/R4).
- `../INTERFACE.md` — the contract (I own it); now carries the real-parts BOM table.

## Interface I expose / consume
- Expose: see `../INTERFACE.md`. Key port for Phase B = `CSP_OUT` (voltage step
  ≈ Q_in × 13 mV/pC + CR-112 decay tail), plus `BIAS_IN`(≤60V), `SIPM`, `TEST_IN`,
  `+12V/−12V/GND`. Schematic handle = U1 `cremat:CR-11X` (CR-112), SIP-8 pinout in INTERFACE.
- Consume: A3 models-bom report (real MPNs) at the Round-2 gate; A2 sim report verifies the
  topology's figures of merit (gain/rise/decay) — informational, no design change unless a
  value moves a FoM.

## How to use my output
Phase B (single-channel) instantiates the front-end + CR-112 + decoupling sub-circuit and
feeds `CSP_OUT` straight into the CR-200 shaper input (drop the standalone J4 output jack and
J5 power entry; the merged channel shares one supply + output). Integrate against
`../INTERFACE.md`.

## Open issues / asks
- **A3 BOM reconciliation (one row):** A3's BOM lists the test-injection cap `Ctest` (1 pF)
  but **omits the 47 Ω series resistor** that the design's test path needs (CR-150-R5 test
  path = 47 Ω + 1 pF). A1 picked a real part from A3's own Yageo RC0805 family —
  **R5 = Yageo RC0805JR-0747RL (47 Ω 0805)** — so the design BOM is fully real. **Ask: A3 /
  coordinator add this one row** so Models-BOM == Design BOM exactly. All other 12 A3 MPNs
  match 1:1.
- **CR-112 lead spec — RESOLVED:** verified against Cremat's own CR-150-R5 board, which lays
  the CR-11X (`8pinSIP`) as 8 inline THT pads at **2.54 mm pitch**, drill 0.762 mm. The
  generic `PinHeader_1x08_P2.54mm_Vertical` (2.54 mm pitch) is the correct real footprint —
  no change.
- **`CONMCX013.step` 3D model missing** (`lib/cremat.pretty/`) — **3D-only, non-blocking**;
  the MCX footprint pads/courtyard are complete so layout/DRC are unaffected. Drop the
  TE/Linx STEP into the pretty lib to restore the 3D view.
- The brief's block diagram and the CR-150-R5 bias network differ (brief = RC+R filter w/
  0R bypass; CR-150 = high-megohm detector bias). I implemented the **brief's** front-end
  topology (authoritative for this build) + CR-150-R5's **module support**. Flagging so the
  coordinator/A2 use the same topology.
